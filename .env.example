OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Supabase (solo backend; nunca en el frontend)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_CV_BUCKET=cv-files

# UUID del usuario en Authentication → Users (obligatorio para FK auth.users)
AGENT_USER_ID=

# Alternativa: el backend crea/busca este usuario con service_role
AGENT_BOOTSTRAP_EMAIL=
AGENT_BOOTSTRAP_PASSWORD=
